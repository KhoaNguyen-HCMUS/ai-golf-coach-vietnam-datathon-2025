import "./index.css"

// The app is now rendered through app/page.tsx instead

// ReactDOM.createRoot(document.getElementById("root")!).render(
//   <React.StrictMode>
//     <App />
//   </React.StrictMode>,
// )
